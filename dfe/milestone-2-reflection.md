# Milestone 2 Reflection — `world-generator`

**Date completed:**
**Link to project folder:** [`month-2/project/world-generator/`](../month-2/project/world-generator/)

> Complete this after Session 16. Aim for 3–5 paragraphs.

---

### What I built

Describe the project in your own words. What does it do? Run it with a couple of different seeds and paste the output if you like — different worlds make a great visual.

---

### Skills I used

List the Rust concepts you applied. From this month, you'll likely have used:

- `struct` and `impl` blocks — how you modelled the `World` and its tiles
- Enums with associated data — how each `Tile` variant carried its own information
- `Vec<Vec<T>>` — your 2D grid
- `Option<T>` and `Result<T, E>` — proper error handling
- Iterators (`.iter()`, `.map()`, `.filter()`, `.collect()`)
- A trait or two (`Display`, maybe a custom one)
- A deterministic noise / hash function — explain how it works in your own words

---

### The hardest part

What was the most challenging thing to build or debug?

---

### What I'm most proud of

Which moment was most satisfying? Maybe the first time the world rendered correctly. Maybe getting the noise function tuned so the biomes looked right.

---

### How this compares to Milestone 1

Look back at `milestone-1-reflection.md`. What feels easier now? What concepts that were new in Month 1 are now second nature? What new tools do you have in your kit?

---

*Signature (participant):* ______________________________
*Date:* ______________________________
